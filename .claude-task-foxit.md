# 任务

在 `D:\uking编程\task-passport` 仓库里新增 `examples/foxit/`，实现一个**端到端的 agent 签署工作流示例**，
用于参加 DevNetwork [API+Cloud+AI] Hackathon 2026 的 Foxit 赛题「Your Agent Shouldn't Sign That」。

## 背景（必读，决定代码怎么写）

本仓库 = Task Passport / TaskPack：长任务状态（目标 / 当前状态 / 已验证事实 / 决策 / 下一步 / ask）
在 AI agent、机器、人之间交接的协议。npm 包 `task-passport@0.3.0`，Node>=20，**纯 JS 零依赖**，
`npm test` 78 项全绿。

Foxit 赛题的设定：他们开源了一个 MCP server，包了 PDF Services API 的 40 个工具
（生成 / 转换 / 合并 / 压缩 / OCR / 提取）——**全部是可逆操作**。
**签名故意不在工具集里**：要送签，agent 必须拿自己的凭据直接调 eSign API，且必须有真人签。
出题人明说欢迎参赛者论证「边界该划在哪」。

**我们的论点（写代码时始终服务于它，README 里要讲清楚）**：

> 把签名从工具目录里拿掉，挡住了 agent 去签——但它**不产生「人为什么说 yes」的记录**。
> 缺的那一半是：让人的授权本身成为一个有版本、可携带、防篡改的一等对象，
> 并且在 agent 崩溃 / 换模型 / 换机器之后依然成立。

所以这个 demo 演的是：**可逆的活 agent 自己干；到了不可逆那一步，它停下来、提出 ask、打包出门、
等人的回执回来、核对回执批的是不是同一份文档，然后才调 eSign。**

## 输入（开工前必须先读）

1. `AGENTS.md` —— 本仓库开机文档，尤其「最原创的那一条」
2. `docs/taskpack-0.1.md` —— 规范正本
3. `core.js` —— 护照状态结构与 Provider 合约
4. `node cli.js --help` —— CLI 全部子命令
5. `examples/gemini/` —— **已有的同类示例，照它的组织方式和文风写**（README 结构、mock 模式、demo.sh）

## 已核实的 Foxit API 事实（照抄，不要自己编端点）

**PDF Services**
- base: `https://na1.fusion.foxit.com/pdf-services`
- 环境变量：`FOXIT_CLOUD_API_HOST` / `FOXIT_CLOUD_API_CLIENT_ID` / `FOXIT_CLOUD_API_CLIENT_SECRET`
- 文档生成：`POST /document-generation/api/GenerateDocumentBase64`（模板 4MB 上限）

**eSign（凭据与 PDF Services 分开，是另一套）**
- base: `https://na1.foxitesign.foxit.com`
- 取 token：`POST /api/oauth2/access_token`，**form-encoded 不是 JSON**，
  字段 `client_id` / `client_secret` / `grant_type=client_credentials` / `scope=read-write`，
  返回 `{"access_token": "..."}`
- 建签署文件夹：`POST /api/folders/createfolder`，body：
  ```json
  {"folderName":"...","inputType":"base64","base64FileString":["<pdf b64>"],
   "fileNames":["agreement.pdf"],"processTextTags":true,"sendNow":false,
   "parties":[{"permission":"FILL_FIELDS_AND_SIGN","firstName":"...","lastName":"...",
               "emailId":"...","sequence":1}]}
  ```
  返回 `{"folder":{"folderId":"..."}}`
- 送签：`POST /api/folders/sendDraftFolder`，body `{"folderId":"..."}`
- 查状态/审计轨迹：`GET /api/folders/viewActivityHistory?folderId=<id>`
  （草稿态会报错 "logs of a non-shared folder can not be viewed"，代码要容忍这个）
- 鉴权 header：`Authorization: Bearer <access_token>`

## 要求

### 硬禁区
- **绝对不许改**：`core.js` `cli.js` `store.js` `mcp.js` `bag.js` `taskpack.js` `outbox.js`
  `index.js` `test/*` `package.json` `docs/*` `site/*` `examples/gemini/*`
- **工作区当前有未提交改动（land --into、outbox.js 等），不许 `git checkout` / `git reset` /
  `git stash`，不许回滚任何已有改动。在现状基础上叠加。**
- **不要 `git add` / `git commit`**——写完就停，由我审 diff 后自己提交
- 只允许新增 `examples/foxit/**`，以及在根 `README.md` **末尾追加**一小节（其他行一字不改）
- 纯 Node 内置模块，**零第三方依赖**（Node 20 有全局 `fetch`、`node:crypto`、`node:fs` 够用）
- 所有 `.mjs` 必须过 `node --check`

### 交付物

**1. `examples/foxit/foxit-pdf.mjs`** —— PDF Services 客户端（薄封装）
- `createPdfFromHtml(html)` → base64 PDF（用 HTML→PDF，**不要往仓库里塞 .docx 二进制**）
- `extractText(pdfB64)` → 文本（用于 agent 自检生成的合同确实含关键条款）
- `compress(pdfB64)` → base64 PDF
- 三个都要有 `--mock` 路径：不调网络，返回确定性的假数据（同样输入 → 同样 sha256）
- 无凭据且非 mock 时报清楚的错，不要静默降级

**2. `examples/foxit/foxit-esign.mjs`** —— eSign 客户端
- `getAccessToken()` / `createFolder({...})` / `sendDraftFolder(folderId)` / `viewActivityHistory(folderId)`
- 同样支持 mock（返回稳定的假 folderId，如 `MOCK-FOLDER-<sha256前8位>`）

**3. `examples/foxit/agent.mjs`** —— 工作流主程序，这是核心。子命令：

- `plan --task "<自然语言任务>" --store <dir>`
  用 `node cli.js new` 建一本护照，标题/目标写进去，打印护照 ID

- `draft --passport <TP-ID> --store <dir>`
  ① 从 `templates/consulting-agreement.html`（自己建，`{{placeholder}}` 占位）渲染合同
  ② 调 `createPdfFromHtml` 生成 PDF，落盘 `out/contract.pdf`
  ③ 调 `extractText` 自检：正文确实含费用条款和服务期 → 记为**本机已验证的事实**
  ④ 每一步都 `node cli.js checkpoint --file <state.json> --expected-version <n>` 写回护照
     （**必须带 expected_version，版本冲突要报错不要重试覆盖**）
  ⑤ 到签名边界**停下**：往护照写一条 ask，然后
     `node cli.js pack <TP-ID> --flat --out out/for-review.taskpack.json --file out/contract.pdf
      --actor "agent" --to "human approver"
      --ask "批准把这份合同送去签署吗？|回执里给出 approve 或 reject，并附上你看到的那份 PDF 的 sha256"`
  ⑥ **退出码 0，但明确打印「我不能签，需要人的批准」**

- `sign --passport <TP-ID> --store <dir> [--signer-email <x> --signer-name "A B"]`
  **这是判分的关键，闸门逻辑一步不能省：**
  1. `node cli.js open <TP-ID>` 读护照
  2. 找那条 ask；**没被回答 → 退出码 3**，打印「没有人批准过，拒绝送签」
  3. 解析回执里的决定；**不是 approve → 退出码 3**
  4. 取回执里的 sha256，和本地 `out/contract.pdf` 现算的 sha256 比；
     **不一致 → 退出码 2**，打印「批准的是 <a>，我手上是 <b>，拒绝送签」
  5. 全过 → 才调 eSign：`createFolder(sendNow:false)` → `sendDraftFolder` → `viewActivityHistory`
  6. 把 folderId、送签时间、审计轨迹 checkpoint 回护照，形成完整责任链
  7. 打印完整责任链摘要

**4. `examples/foxit/review.mjs`** —— 人这一侧（**薄壳，不要重新实现 pack 解析，走 cli.js**）
- `show <pack.json>`：人类可读地打印——agent 做了哪几步、它声称的事实里
  **哪些在你这台机器上是「未证」**、文档 sha256 是多少、它在问什么
- `approve <pack.json> --into-store <human-store> --actor "<name>" [--reject]`：
  `land` 进人这边的 store → 写决定 → `pack ... --kind receipt --flat --out out/receipt.taskpack.json`

**5. `examples/foxit/demo.sh`** —— 全 mock 端到端，**必须用两个独立 store 模拟两台机器**：
`.demo/agent-store` 和 `.demo/human-store`。
（同一个 store 演不出封存规则——agent 在自己机器上验过的事实，land 到人那边必须显示为未证，
这是本仓库最原创的一条，demo 必须让它可见。）

流程：`plan` → `draft` → `review.mjs show`（**打印出未证的那几条**）→ `approve` →
`cli.js land receipt --into <agent-TP-ID>` → `agent.mjs sign` → 打印责任链。

再加两个反向用例，脚本里跑完要显式打印 PASS/FAIL：
- `demo.sh --no-approval`：跳过人这一步直接 sign → **必须退出码 3**
- `demo.sh --tamper`：批准之后偷偷改 `out/contract.pdf` 再 sign → **必须退出码 2**

**6. `examples/foxit/templates/consulting-agreement.html`** —— 带 `{{client_name}}`
`{{monthly_fee}}` `{{term_months}}` 等占位符的合同模板

**7. `examples/foxit/README.md`** —— 结构照 `examples/gemini/README.md`，必须包含：
- 赛题是什么、**我们的论点**（上面那段，用自己的话展开，别照抄）
- 架构图（ASCII 即可）：自然语言 → 护照 → Foxit 可逆工具 → **ask 边界** → 打包出门 →
  人审 → 回执 → 核对哈希 → eSign → 回执 land 回护照
- 40 个可逆工具 vs 1 个不可逆动作，为什么边界画在这里
- 免凭据的 mock 演示步骤（一条命令）
- 真实凭据配置（PDF Services 与 eSign **是两套凭据**，写清楚）
- **「这个 demo 不证明什么」小节（必写，本仓库的规矩）**：
  台账/回执不是公证；能写 store 的人就能改它；它证明的是「批准的是哪一版、谁批的」，
  不是「这个人真的存在过」。不许吹成防伪。

**8. 根 `README.md` 末尾追加** `## Foxit eSign 集成（Hackathon）` 小节，3-6 行 + 链接到
`examples/foxit/README.md`。**README 现有内容一字不改，只追加。**

## 输出

全部落在 `examples/foxit/`（+ 根 README 追加）。
另外把验收执行结果写到 `D:\uking编程\task-passport\.hermes-foxit-report.md`。

## 验收（逐条真跑，把真实输出贴进报告，不许编）

- [ ] a) `npm test` 仍 78 全绿（一项都不能少）
- [ ] b) `npm run check` 通过
- [ ] c) 每个新增 `.mjs` 过 `node --check`
- [ ] d) `bash examples/foxit/demo.sh` 在 git-bash 里跑通，输出里能看到：
      护照版本号递增、ask 的原文、pack 文件生成、**review 打印出「在你这台机器上未证」的事实**、
      回执 land 回去、最终 eSign folderId、责任链摘要
- [ ] e) `bash examples/foxit/demo.sh --no-approval` 退出码 **3**
- [ ] f) `bash examples/foxit/demo.sh --tamper` 退出码 **2**
- [ ] g) `git status` 只有 `examples/foxit/` 新增 + `README.md` 被改（且 `git diff README.md` 只有追加）
- [ ] h) 没有任何 git 提交动作

拿不准的地方**别猜**：把决策点列在报告末尾「需要 Claude 定的」一节里，先把不冲突的部分做完。
这是单发任务，做完直接结束。
