你是本仓库临时开发协作者。仓库 D:\uking编程\task-passport = Task Passport / TaskPack：长任务状态（目标/当前状态/已验证事实/决策/下一步/ask）在 AI agent、机器、人之间交接的协议。Agent A 崩溃/关机/换模型时 pack 成文件，Agent B 用 land 收下继续——「agents shouldn't restart from zero」。npm 包 task-passport@0.3.0，Node>=20 纯 JS，78 测试全绿（npm test）。

任务：写 Google/Gemini 集成示例（比赛硬性要求：Gemini 3.5+ 经 Gemini API 或 Vertex AI + Google Agent Framework + GCP 服务）。全部放 examples/gemini/ 新目录，绝对不许改 core.js/cli.js/store.js/mcp.js/bag.js/taskpack.js/outbox.js/index.js/test/*/package.json。

交付物：
1. examples/gemini/adk_agent.py —— 基于 Google ADK (pip 包 google-adk) 的 Agent B 接收端：子命令 land <包文件>/open/continue <指令>/pack --out <文件> --actor <名>，内部用 subprocess spawn 调 `node cli.js`（Node CLI 从仓库根目录跑，node 是原生 Windows 程序，路径用 C:/ 正斜杠风格）。ADK Agent + @tool 装饰器注册 land/open/checkpoint/pack 为模型可调用工具。必须支持 --mock：不调 Google API 也能完整跑通「收包→读状态→执行一步→写回→封包」链路（mock 用自定义 RuleBasedModel 固定返回工具调用，或顺序执行）。真实模式用 google-genai 连 Gemini。附 requirements.txt（google-adk, google-genai）。

2. examples/gemini/firestore-provider.mjs —— Google Cloud Firestore 作为护照 Provider 示例（实现 list()/open(id)/save(state, expectedVersion) 三个方法，见 core.js 的 createPassportClient 合约），main 示例跑 new→open→checkpoint 最小闭环。支持 FIRESTORE_EMULATOR_HOST 环境变量用本地 emulator（免 key 演示）。附 package.json（依赖 @google-cloud/firestore）。

3. examples/gemini/README.md —— 集成总说明：三硬性要求怎么满足（Gemini=模型、ADK=框架、Firestore=GCP 服务）、GEMINI_API_KEY/Vertex 配置、emulator 免 key 演示步骤、一键演示、「Agent B=Gemini 交接」叙事（Agent A 任意 harness 打包 → Gemini ADK agent 收下继续 → 封包发回 → 任意 harness land 收尾）、诚实边界（示例性质，核心零 Google 依赖）。

4. README.md 末尾追加「## Google / Gemini 集成（Hackathon）」小节：3-5 行 + 链接 examples/gemini/README.md。README 其他内容一字不改（README 当前有未提交改动，必须在其基础上追加，用 git diff 看现状，不能覆盖）。

已验证的真实配置（写进 README 和代码默认值，别用占位符）：
- Vertex 端点：https://asia-northeast1-aiplatform.googleapis.com（gemini-3.5-flash 在此区域真调通；us-central1 只有 2.5 系列）
- 项目 clawme-488709 / 区域 asia-northeast1 / 模型 gemini-3.5-flash
- key 从环境变量 GEMINI_API_KEY 或 GOOGLE_API_KEY 读；未设置且非 mock 时报错提示
- REST 实测格式：POST /v1/projects/{proj}/locations/asia-northeast1/publishers/google/models/gemini-3.5-flash:generateContent，header x-goog-api-key: <key>，body {"contents":[{"role":"user","parts":[{"text":"..."}]}]}

验收（做完逐条执行并贴出真实输出）：
a) npm test 78 全绿
b) npm run check 通过
c) demo 脚本 mock 模式在 git-bash 跑通：node cli.js 造护照→pack→adk_agent.py --mock land→open→continue→pack 回传，状态版本/文本变化可见
d) firestore-provider.mjs 过 node --check
e) git status 只新增 examples/gemini/ 与 README 追加

先读 README.md 和 core.js（Provider 合约），再写代码。完成后输出验收结果。这是单发任务，做完直接结束。