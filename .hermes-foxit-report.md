# Hermes 验收报告 — examples/foxit（Foxit eSign Hackathon demo）

- 执行者：Hermes（本机，2026-08-19）
- 任务文件：`.claude-task-foxit.md`
- 交付：`examples/foxit/**` 7 个新文件 + 根 `README.md` 末尾追加 1 小节
- 验收方式：全部真跑，以下输出均为实际命令输出，无编造

## 一、交付物清单

| 文件 | 说明 |
| --- | --- |
| `examples/foxit/foxit-pdf.mjs` | PDF Services 客户端：createPdfFromHtml / extractText / compress，各有确定性 --mock 路径；无凭据且非 mock 报清楚 |
| `examples/foxit/foxit-esign.mjs` | eSign 客户端：getAccessToken / createFolder / sendDraftFolder / viewActivityHistory；mock folderId=`MOCK-FOLDER-<sha256 前 8 位>`；草稿态审计报错已容忍 |
| `examples/foxit/agent.mjs` | 工作流主程序：plan（开护照）/ draft（渲染→PDF→自检→checkpoint×3→ask→pack，退出码 0 但「我不能签」）/ sign（四道闸门：有人批→是 approve→sha256 一致→才 eSign，退出码 2/3 是判分口径） |
| `examples/foxit/review.mjs` | 人这一侧薄壳：show（未证事实 / sha256 / ask 原文）/ approve（land→写决定→打回执，支持 --reject），全走 cli.js |
| `examples/foxit/demo.sh` | 全 mock 端到端，两个独立 store 模拟两台机器；`--no-approval` 退出码 3、`--tamper` 退出码 2，显式打印 PASS/FAIL |
| `examples/foxit/templates/consulting-agreement.html` | 带 `{{client_name}}` `{{monthly_fee}}` `{{term_months}}` 等占位符的合同模板 |
| `examples/foxit/README.md` | 论点 / ASCII 架构图 / 边界论证 / 一条命令 mock 演示 / 两套凭据 / 「这个 demo 不证明什么」 |
| 根 `README.md` | 末尾追加「Foxit eSign 集成（Hackathon）」小节（3-6 行 + 链接），现有行一字未改 |

## 二、验收逐条（真实输出）

### a) `npm test` 仍 78 全绿 ✅

```
# tests 78
# pass 78
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 1600.9122
```

### b) `npm run check` 通过 ✅

```
> task-passport@0.3.0 check
> node --check bag.js && node --check taskpack.js && node --check core.js && node --check store.js && node --check outbox.js && node --check index.js && node --check mcp.js && node --check cli.js
CHECK EXIT=0
```

### c) 每个新增 .mjs 过 `node --check` ✅

```
OK examples/foxit/agent.mjs
OK examples/foxit/foxit-esign.mjs
OK examples/foxit/foxit-pdf.mjs
OK examples/foxit/review.mjs
```

### d) `bash examples/foxit/demo.sh` 跑通（退出码 0）✅

完整日志 104 行（见 `.demo-run-final.log`，本轮验收后已删除），验收要求的要素逐一出现：

**护照版本号递增**：
```
护照版本 → 4（渲染 / PDF / 自检各一次 checkpoint）
护照版本 → 5（ask 已记录，asks_recorded=true）
（land 回执后 state_version 6，sign 责任链 checkpoint 后 → 版本 7）
```

**ask 原文 + pack 文件生成**：
```
[4/4] 到签名边界，停下。打包出门 → …\.demo-foxit\out\for-review.taskpack.json
     ask: 批准把这份合同送去签署吗？|回执里给出 approve 或 reject，并附上你看到的那份 PDF 的 sha256
```

**review 打印出「在你这台机器上未证」的事实**（本仓库最原创的一条，双 store 可见）：
```
—— agent 声称的事实（区分「可信」与「在你这台机器上未证」）——
  ⚠️ 未证  [machine] 合同 PDF …（sha256=da03fc0b…e34d49）含费用条款 ¥5000 和服务期 12 个月
        曾在 zjzhfs 上验证过——那是另一台机器，打包时已封存。
        在你这台机器上先重验再用，别当事实。
```

**回执 land 回去**：
```
{"ok":true,"merged_into":"TP-WHTE-2EJU","state_version":6,…,"answered":["a1"],"answered_count":1,…}
回执已 land 回 TP-WHTE-2EJU
```

**最终 eSign folderId + 责任链摘要**：
```
闸门4 eSign ✓ folderId=MOCK-FOLDER-2a87db28 送签于 2026-08-19T12:43:43Z
========== 责任链摘要 ==========
护照        : TP-WHTE-2EJU（版本 7）
… 回答: approve sha256=da03fc0b…（张老师@zjzhfs @ 2026-08-19T12:43:43Z）
闸门①有人批  : ✓ approve
闸门②版本一致: ✓ 批准 da03fc0b… == 本机现算 da03fc0b…
eSign       : folderId=MOCK-FOLDER-2a87db28，送签于 …
审计轨迹    : FOLDER_CREATED@… → DRAFT_READY@… → SENT@…
```

### e) `bash examples/foxit/demo.sh --no-approval` 退出码 3 ✅

```
===== 反例①：跳过人这一步，直接 sign =====
没有人批准过，拒绝送签。
ask a1 状态=open；回执 land 回来之前，签名这道门不开。
✅ PASS：没有人批准，agent 拒绝送签（退出码 3 = 3）
```
实测 `echo $?` = **3**。

### f) `bash examples/foxit/demo.sh --tamper` 退出码 2 ✅

```
===== 反例②：批准之后偷偷改 out/contract.pdf，再让 agent 签 =====
（已向 …\out\contract.pdf 追加字节）
批准的是 da03fc0b13086186081758fba77d8977457c049d40d0024beeb1d12b04e34d49，我手上是 daf86585effa6cc45e10491de79fc76a7159f5b254399227393194c5b30f0c53，拒绝送签。
这两份不是同一版文档——回执批的是另一份，签名绝不能落在这一份上。
✅ PASS：批准的是旧版本，agent 拒绝送签（退出码 2 = 2）
```
实测 `echo $?` = **2**。

### g) `git status` / `git diff README.md` ✅

`git status --short` 里由本任务引入的只有：
- `?? examples/`（其中新增 `examples/foxit/` 7 个文件；`examples/gemini/` 是任务开始前就有的未跟踪目录）
- `M README.md`（任务开始前已是 M——工作区原本就有 land --into、outbox 等未提交改动；本任务只在其末尾追加）

`git diff README.md`：`27 insertions(+), 0 deletions(-)`（唯一的 `-` 行是 diff 头 `--- a/README.md`），纯追加，现有内容一字未改。

任务开始前已存在的其他 M/??（bag.js cli.js core.js store.js test/* .video-demo/ videos/ 等）未做任何改动、未回滚、未 add。

### h) 没有任何 git 提交动作 ✅

全程未执行 `git add` / `git commit` / `checkout` / `reset` / `stash`；`git log --oneline -1` 仍停在 `da74176`（任务开始前的 HEAD）。

## 三、测试期间发现并修复的问题

1. `agent.mjs` 顶层 `await` 放在普通函数里 → 语法错误。改为 `async function cmdDraft/cmdSign` + main 里 `await`。
2. `demo.sh` 把 `"node examples/foxit/agent.mjs"` 存成变量再整体调用 → bash 把整串当可执行文件名（exit 127）。改为函数 `agent() { node examples/foxit/agent.mjs "$@"; }`。
3. `demo.sh` 反例分支原先打印 PASS 后 `exit 0`；按任务要求「demo.sh --no-approval 退出码 3 / --tamper 退出码 2」改为 PASS 后透传退出码 `exit 3` / `exit 2`。
4. `review.mjs show` 原打印「发给」行，但 `--to` 只进发件台账、不进包体（包里 ask.to 恒为 peer），删掉避免误导。
5. `agent.mjs draft` 补了 `next_steps: ['等待人的回执 land 回来…后，执行 sign 送签']`，让 show 里「agent 的下一步」指向正确动作。

## 四、需要 Claude 定的决策点（拿不准的地方，先按下列默认实现，都集中在可单点修改处）

1. **回执的 root_id 处理（最重要）**：`mergeReceipt` 强制要求 receipt 的 `lineage.root_id` 等于目标护照 id（`bag.js:370`），而 `land` 会新开一本护照。`review.mjs approve` 的默认实现是：land 进 human store → 把新护照的 `state.id` 改回包的 `lineage.root_id` 再 checkpoint（expected-version 0，因为 human store 里尚无该 id 的文件）→ 用该 id 打 receipt。副作用：human store 里会出现一本以原 id 命名的「人的决定」护照，land 产生的中间护照文件会被删掉。若你更希望 human store 保持「新 id 护照 + 手工把答案带回」，改动集中在 `review.mjs cmdApprove`。
2. **PDF Services 真实模式的鉴权形态**：任务事实只给了端点，没给 PDF Services 的 token 获取方式。默认用 OAuth2 客户端凭证的 HTTP Basic（`client_id:client_secret`），可用 `FOXIT_PDF_ACCESS_TOKEN` 覆盖为 Bearer；`extractText` / `compress` 的真实端点不在已核实事实里，默认要求显式 `FOXIT_EXTRACT_ENDPOINT` / `FOXIT_COMPRESS_ENDPOINT`，没给就报清楚。拿到凭据后需一次真实校准。
3. **eSign 环境变量名**：任务只给了 PDF Services 的三个变量名，eSign 那套是我定的 `FOXIT_ESIGN_HOST` / `FOXIT_ESIGN_CLIENT_ID` / `FOXIT_ESIGN_CLIENT_SECRET`（README 已写明两套凭据）。
4. **ask 回答格式**：定为 `approve sha256=<64hex>` / `reject sha256=<64hex>`，与 accept 规则「回执里给出 approve 或 reject，并附上你看到的那份 PDF 的 sha256」对应；sign 用正则解析，解析失败按「非 approve」处理（退出码 3）。
5. **`draft`/`sign` 的 `--out` 参数**：默认 `out/`，demo 传 `.demo-foxit/out`（仓库根不留 `out/`，避免污染 `git status`）。
6. **demo.sh 反例的退出码语义**：脚本打印 PASS 后以 3/2 退出（任务验收要求命令本身退出码 3/2）；happy path 退出码 0。

## 五、遗留风险

- 真实模式未联网验证（无凭据；`.devpost-foxit/submission-plan.md` 里「发邮件要 eSign 凭据」仍在等你确认）。
- `--reject` 分支已实现但 demo.sh 未演示（任务只要求 approve 路径 + 两个反例）；可手动跑：`review.mjs approve … --reject` 后 sign 应退出码 3。

## 六、后续校准（2026-08-22，PDF Services 真实模式已闭环）

拿到 Sandbox 凭据后，用真凭据探针 + 网关自带的 OpenAPI 规范（`GET /pdf-services/v3/api-docs`）
把 PDF Services 的真实行为全部摸清并校准了代码：

| 项 | 任务文档旧表述 | 实测事实（以实测为准） |
| --- | --- | --- |
| 鉴权 | 未给（代码原先按 HTTP Basic + Bearer 覆盖） | **client_id / client_secret 作为两个独立请求头**；服务端缺一个就回 "Missing credentials: provide both 'client_id' and 'client_secret' headers."。x-api-key 不需要（OpenAPI securityScheme 与真实网关不符） |
| 生成端点 | `POST /document-generation/api/GenerateDocumentBase64` | 该路径 404（"No static resource"）；真实流程是 **multipart 上传 → 异步任务 → 下载** |
| 真实流程 | — | `POST /api/documents/upload`（multipart 字段 `file`）→ documentId → `POST /api/documents/create/pdf-from-html`（返回 202 {taskId}）→ `GET /api/tasks/{taskId}` 轮询（PENDING/IN_PROGRESS/COMPLETED/FAILED）→ resultDocumentId → `GET /api/documents/{id}/download` |
| 提取 | 端点未给（原要求 `FOXIT_EXTRACT_ENDPOINT`） | `/api/documents/convert/pdf-to-text`，完成后的 resultDocumentId 是一个 .txt 文档，下载即文本 |
| 压缩 | 端点未给（原要求 `FOXIT_COMPRESS_ENDPOINT`） | `/api/documents/modify/pdf-compress`，body `{documentId, compressionLevel}`（默认 MEDIUM，可用 FOXIT_COMPRESS_LEVEL 覆盖） |

**改动**：`examples/foxit/foxit-pdf.mjs` 真实模式重写（upload → 操作 → 轮询 → download，
`authHeaders()` 双 header；删除 Basic / FOXIT_PDF_ACCESS_TOKEN / FOXIT_EXTRACT_ENDPOINT /
FOXIT_COMPRESS_ENDPOINT 等旧假设）；**mock 路径一字未动**。README 的「真实凭据配置」
「手动走一遍」「不证明什么」三节同步更新；OpenAPI 规范存进 `.devpost-foxit/foxit-openapi.json`（gitignore 内）。

**验证（全部真跑）**：
- `node --check examples/foxit/foxit-pdf.mjs` ✓；无残留旧引用（grep 确认）
- `npm test` 78/78 ✓；`npm run check` ✓
- `bash examples/foxit/demo.sh` 三个剧本全 PASS（mock 确定性 sha256 不变）✓
- **真实模式 agent 全链路**：`agent.mjs plan → draft`（不带 --mock）用真凭据跑通：
  真实生成的 contract.pdf（`sha256=ff9ac163…`）→ 真实 extractText 自检「含费用条款 ✓
  含服务期 ✓」→ checkpoint ×3 → ask 打包 → 「🚫 我不能签，需要人的批准」退出码 0 ✓

**eSign 侧已同日闭环（2026-08-22，用户自助激活）**：
- 控制台 eSign API → Activate（US storage），Account #2907377，与 PDF Services 同一套
  统一平台凭据（无 OAuth token，旧 foxitesign 域名 + token 流程是遗留 API）。
- 实测端点：na1.fusion.foxit.com + /esign/api/v1/{createfolder,sendDraftFolder,
  viewActivityHistory}；createfolder 必须带 signature fields，否则 sendDraftFolder 拒绝。
- 完整真实演示跑通：真 PDF → ask → 人批 → land → sign 送签 folderId=35504526，
  签署邀请邮件已到达收件人 Gmail（notifications@foxitsign.com）。
- foxit-esign.mjs 已按实测重写（headers 鉴权、signature 域、统一主机；mock 未动）。
- 遗留小项：Foxit 账号公司名显示为 "Independent Develope"（少个 r），影响封套发件人名。
